# MinIO

MinIO stores ML model artifacts in local demo mode. The AI service points MLflow at `MLFLOW_TRACKING_URI` and can use `MINIO_ENDPOINT` as an S3-compatible artifact backend when credentials are provided.

